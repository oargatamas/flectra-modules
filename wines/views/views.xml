<flectra>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wines.list">
      <field name="name">wines list</field>
      <field name="model">wines.wines</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wines.action_window">
      <field name="name">wines window</field>
      <field name="res_model">wines.wines</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wines.action_server">
      <field name="name">wines server</field>
      <field name="model_id" ref="model_wines_wines"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="wines" id="wines.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wines.menu_1" parent="wines.menu_root"/>
    <menuitem name="Menu 2" id="wines.menu_2" parent="wines.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wines.menu_1_list" parent="wines.menu_1"
              action="wines.action_window"/>
    <menuitem name="Server to list" id="wines" parent="wines.menu_2"
              action="wines.action_server"/>
    -->
  </data>
</flectra>